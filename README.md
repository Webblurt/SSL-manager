# SSL-manager
API to generate and update certificates 

Neccessary

create folder according to the config Certs.StorageDir
give rights

sudo mkdir -p /etc/ssl/autocert
sudo chown myappuser:myappuser /etc/ssl/autocert
sudo chmod 700 /etc/ssl/autocert
